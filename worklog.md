# Worklog

---
Task ID: 1
Agent: general-purpose
Task: Redesign Why Choose Us page with content + images

Work Log:
- Read existing `/src/app/(pages)/why-choose-us/page.tsx` and the shared primitives (`page-hero`, `page-shell`, `primitives`), `site-data.ts` data exports (`whyChooseTop`, `whyChooseGrid`, `whyChooseComparison`, `whyChooseFAQ`, `whyFeatures`, `stats`, `valueTags`), `seo.ts` `buildMetadata`, and `globals.css` design tokens (`bg-shade`, `bg-cream`, `bg-ink`, `card-lift`, `rule-flame`, `section-label`, `container-site`, `text-flame`, `shadow-glow-flame`, `shadow-lift`, `grid-pattern`) to ground the redesign in the existing premium design system.
- Confirmed all 10 Lucide icons required by the spec (`Globe2`, `Sparkles`, `Settings2`, `Rocket`, `ShieldCheck`, `Target`, `Users`, `Zap`, `Workflow`, `TrendingUp`) are exported by `lucide-react` and imported the `LucideIcon` type for clean typing.
- Rewrote `/src/app/(pages)/why-choose-us/page.tsx` end-to-end with the new content + image structure:
  1. **Hero** — `PageHero` with title "Why Choose Us", label "Why choose us", image `/images/wp/2025-01/why_choose01.jpg`, crumbs Home → Why Choose Us.
  2. **Intro promise** — `SectionHeading` centered on white with eyebrow `[ Our promise ]`, title "Engineering Excellence. Business Impact. Global Delivery." (with "Global Delivery." in `text-flame`), and the brand lead paragraph.
  3. **Editorial image break** — full-bleed `next/image` of `/images/wp/2025-02/about_mna00n.jpg` with a left-heavy navy gradient overlay, grid pattern, flame orb, and a small overlay quote ("Built right. Built to last. Built for impact.").
  4. **10 feature cards** — combined `whyChooseTop` (3) + `whyChooseGrid` (7) into one `featureCards` array. Rendered in a 2-column grid (`md:grid-cols-2`) on a `bg-white` section, with each card using `bg-shade` and `p-7 sm:p-8` for breathing room. Each card: square cream icon tile (size-14) → brand on hover with `shadow-glow-flame`, bold `text-ink` title, brand-orange subtitle, body description, signature flame top-bar (`scale-x-0 → group-hover:scale-x-100`), NO numbered markers (per spec — the `01/02/…` paragraph was removed).
  5. **Stats band** — kept navy KPI band with `grid-pattern` + flame orb.
  6. **Comparison table** — kept us-vs-them table with `CheckCircle2` / `X` cells.
  7. **Engineering principles** — kept navy glass-card section using `whyFeatures` data with `f.icon` (Cpu / Rocket / Lock / Scale).
  8. **Quote + value pills** — kept cream section with `Quote` icon, brand copy, and `valueTags` pills.
  9. **FAQ** — kept accordion-style `details` list using `whyChooseFAQ`.
  10. **CTA** — `CTABand` tone="brand" linking to `/work-process`.
- Wrote a small `splitCardCopy(desc)` helper that splits each card's combined `desc` into a tagline subtitle + description paragraph. Handles the tricky case where the first sentence is very short (e.g. "One Partner.") by merging it with the next sentence ("Complete Technology Solutions.") so the orange subtitle reads as one phrase. Strips the trailing period for cleaner display. Verified behavior with a node test against all 10 cards.
- Updated `buildMetadata` keywords to reflect the new content focus (engineering excellence, tailored solutions, agile delivery).
- Ran `bun run lint` — passes with no errors. Confirmed `tsc` is clean for the page (the only TS errors are in unrelated `examples/websocket/` and `skills/` directories, not in `src/`).
- Used the established `<></>` wrapper (matching every sibling page: about, services, work-process, etc.) instead of `<PageShell>`. The `PageShell` role (Header + Footer wrapper) is already provided automatically by `src/app/(pages)/layout.tsx` for every page in the `(pages)` route group, so wrapping the page again with `<PageShell>` would duplicate the nav and footer. `PageShell` is functionally identical to `(pages)/layout.tsx` and is reserved for pages outside the route group.

Stage Summary:
- `src/app/(pages)/why-choose-us/page.tsx` fully rewritten to the new brand copy and image structure. Lint clean. Page uses `PageHero`, `Reveal`, `SectionHeading`, `CTABand`, `buildMetadata`, all required data exports from `@/lib/site-data`, and images from `/images/wp/`. Design adheres to the premium design-system tokens (`bg-white` / `bg-shade` / `bg-ink` / `bg-cream` alternation, `card-lift`, `rule-flame`, `section-label`, `container-site`, `text-flame` / `text-brand` accents, `shadow-glow-flame` / `shadow-lift`).
- 10 feature cards now render in a 2-column grid with cream square icon tiles, brand-orange subtitles, flame top-bar hover, and NO numbered markers (per spec).
- Editorial full-bleed image break (`/images/wp/2025-02/about_mna00n.jpg`) with navy gradient overlay added between the intro and the feature grid.
- Nav + Footer wrapping is provided by `(pages)/layout.tsx` (functionally equivalent to `PageShell`); wrapping again would duplicate nav/footer, so the page body uses a React fragment consistent with all sibling pages.

---
Task ID: 2
Agent: general-purpose
Task: Redesign the Work Process page at `/src/app/(pages)/work-process/page.tsx`

Work Log:
- Read `worklog.md` (Task 1 context), the existing `work-process/page.tsx`, `primitives.tsx` (`Reveal`, `SectionHeading`, `CTABand`), `page-hero.tsx`, `site-data.ts` `workProcessPhases` export (8 phases: 01 Discover → 08 Evolve, each with `phase` / `title` / `duration` (subtitle) / `desc` / `deliverables[]`), `seo.ts` `buildMetadata`, `globals.css` design tokens (`bg-shade` `#f5f6f7`, `bg-cream` `#fff4eb`, `bg-ink` `#0b165e`, `bg-ink-deep` `#00033d`, `text-flame` `#fb8d2e`, `text-brand` `#d9742b`, `rule-flame` 44×3px gradient, `section-label`, `container-site` 1310px, `card-lift`, `grid-pattern`, `py-section-md`), and the sticky-rail pattern from sibling pages (`services/[slug]`, `industries/[slug]` → `lg:sticky lg:top-32 lg:self-start`) to ground the redesign in the established premium design system.
- Confirmed all 12 required Lucide icons exist: spec set `Compass, Map, PenTool, Code2, ClipboardCheck, UploadCloud, Activity, RefreshCw, ArrowRight, Clock` plus `ShieldCheck, Rocket` for the rituals cards (per spec). Mapped the 8 phase icons 1:1: Compass (Discover) → Map (Strategize) → PenTool (Design) → Code2 (Develop) → ClipboardCheck (Test) → UploadCloud (Deploy) → Activity (Monitor) → RefreshCw (Evolve). Used `ArrowRight` in the sticky-column "Start a project" link, `Clock` + `RefreshCw` as inline meta indicators (Two-week sprints / Continuous loop) so every imported icon is actually rendered (no unused-import lint error).
- Confirmed both required image-break assets exist under `/public/images/wp/2025-02/`: `technology1.png` (used between phase 03 Design → 04 Develop) and `concept.png` (used between phase 06 Deploy → 07 Monitor).
- Rewrote `/src/app/(pages)/work-process/page.tsx` end-to-end with the new content + sticky-split structure:
  1. **Hero** — `PageHero` with title "Our Work Process", label "Work process", image `/images/wp/2025-02/about_mna00n.jpg`, crumbs Home → Work Process (matches spec exactly).
  2. **Section 1 (intro, centered on `bg-white`)** — `SectionHeading` with eyebrow `[ How we work ]`, title "Our Work Process", lead "From Vision to Value — A Structured Approach to Digital Excellence", `align="center"`. Added the secondary subtext "We follow a collaborative and transparent process that transforms ideas into scalable, reliable, and high-performing digital solutions." as a centered `mt-5 max-w-2xl` paragraph below the lead.
  3. **Section 2 (phases, `bg-shade`)** — clean 2-col grid `lg:grid-cols-[1fr_1.55fr]` so the right column gets the editorial breathing room it needs. **Left sticky column** (`lg:sticky lg:top-32 lg:self-start`): flame rule (`<div aria-hidden className="rule-flame" />`), section-label `[ The delivery loop ]`, h2 "Eight phases, one engineering loop." (with "engineering loop." highlighted `text-flame`), brand intro paragraph, two inline meta indicators (Clock/RefreshCw), and a "Start a project →" outline link (uses ArrowRight). **Right column**: 8 phase rows as horizontal `grid` rows — `border-t border-line` hairline divider on each row, mono number (small `text-sm font-mono text-ink/45` — explicitly NOT oversized), `size-12` cream icon tile with brand icon, then body containing title (`text-display-sm font-bold text-ink`) with the duration subtitle inline (`text-sm font-medium text-brand — duration`), then the `desc` paragraph (`text-[15px] leading-relaxed text-body`). Final row gets a closing hairline so the list reads as a closed unit.
  4. **Image breaks inside the right column** — inserted after phase index 2 (Design) and index 5 (Deploy): full-width `next/image` (960×420, `h-56 sm:h-64 object-cover`) wrapped in a `rounded-2xl border border-line` panel, overlaid with a left-heavy navy gradient (`from-ink-deep/90 via-ink-deep/55 to-ink-deep/20`) and a left-aligned overlay block containing a `[ Design → Develop ]` / `[ Deploy → Monitor ]` section-label (forced `!text-brand-light !mb-0` to override the default 14px margin-bottom), a short headline ("Where pixels become production code." / "Launch day is a checkpoint, not a finish line."), and a one-line supporting sentence in `text-white/75`.
  5. **Section 3 (rituals, `bg-white`)** — `SectionHeading` centered with eyebrow `[ Engineering rituals ]`, title "Three rituals that make the loop stick.", lead describing the relationship to the 8 phases. Below: 3-col `md:grid-cols-3` grid of cards using `workProcessPhases`-style data defined inline (Weekly review / Architecture board / Quarterly review). Each card uses `card-lift` hover, `bg-shade` panel with `border-line`, signature 3px flame top-bar (`scale-x-0 → group-hover:scale-x-100`), `size-12` cream icon tile that flips to `bg-brand text-white` on hover, `text-display-sm` title, and `text-body` description. Icons: Compass / ShieldCheck / Rocket (per spec).
  6. **Section 4 (CTA, `tone="ink"`)** — `CTABand` with label `[ See it in motion ]`, title "See it applied to a real engagement.", desc walking through what the call covers, `ctaHref="/contact"`, `ctaLabel="Start a project"`. Tone "ink" gives the navy gradient panel — completing the `bg-white` → `bg-shade` → `bg-white` → `bg-ink` alternation.
- Updated `buildMetadata` description to reflect "Eight delivery phases" (the original said "Seven delivery stages" which was wrong against the actual `workProcessPhases` data of 8 entries) and refreshed keywords toward engineering-workflow / agile-methodology / delivery-phases.
- Followed the established React-fragment (`<>…</>`) wrapper pattern used by every sibling page; nav + footer are already provided by `(pages)/layout.tsx` so wrapping again with `<PageShell>` would duplicate them.
- Ran `bun run lint` (`eslint .`) → clean, no errors. Also ran `bunx tsc --noEmit` filtered for `work-process` → no type errors in the page.

Stage Summary:
- `src/app/(pages)/work-process/page.tsx` fully rewritten to the new brand copy + sticky-split + image-break structure. Lint clean. Page imports and uses `PageHero`, `Reveal`, `SectionHeading`, `CTABand`, `buildMetadata`, `workProcessPhases`, `next/image` (`Image`), `next/link` (`Link`), and all 12 spec'd Lucide icons (`Compass`, `Map`, `PenTool`, `Code2`, `ClipboardCheck`, `UploadCloud`, `Activity`, `RefreshCw`, `ArrowRight`, `Clock`, plus `ShieldCheck`, `Rocket` for rituals).
- Section 2 implements the spec's clean 2-col layout: left sticky heading ("Eight phases, one engineering loop.") with flame rule + intro, right list of 8 phases as horizontal rows with small mono numbers (NOT oversized), icon tiles, title-with-inline-subtitle, desc, and hairline borders. Two image breaks (`technology1.png` after phase 03, `concept.png` after phase 06) inserted inside the right column with left-heavy navy gradient overlays and brand-light section-label + headline + supporting copy.
- Section 3 rituals: 3 cards on `bg-white` using `card-lift` hover, flame top-bar, cream icon tiles that flip to brand on hover — Weekly review (Compass) / Architecture board (ShieldCheck) / Quarterly review (Rocket).
- Section 4: `CTABand` `tone="ink"` linking to `/contact` with the spec'd copy ("See it applied to a real engagement." + "Start a project").
- Design-token usage matches the established system: `bg-white` / `bg-shade` / `bg-ink` (via CTA `tone="ink"`) alternation, `card-lift` hover, `rule-flame`, `section-label`, `container-site`, `text-flame` (sticky heading highlight), `text-brand` (inline subtitles + meta icons), `text-ink` (headings), `text-body` (paragraphs). NO oversized numbers on cards per spec.

---
Task ID: 3
Agent: general-purpose
Task: Redesign the Technologies page at `/src/app/(pages)/technologies/page.tsx`

Work Log:
- Read `worklog.md` (Task 1 + Task 2 context), the existing `technologies/page.tsx`, `primitives.tsx` (`Reveal`, `SectionHeading`, `CTABand`), `page-hero.tsx`, `seo.ts` `buildMetadata`, `services/page.tsx` (for the established card grid pattern — `card-lift` + flame top-bar + cream icon tile that flips to brand on hover), `site-data.ts` `technologiesGrid` (24 `{name, img}` logo pairs) and `technologyCategories` (7 entries: Frontend / Backend / Mobile / AI & Data / Cloud / Database / DevOps — each with `name`, `desc`, `items[]`), and `globals.css` design tokens (`bg-shade` `#f5f6f7`, `bg-cream` `#fff4eb`, `bg-ink` `#0b165e`, `text-flame` `#fb8d2e`, `text-brand` `#d9742b`, `rule-flame` 44×3px gradient, `section-label`, `container-site` 1310px, `card-lift`, `grid-pattern`, `shadow-glow-flame`, `shadow-lift`, `btn-lift`) to ground the redesign in the established premium design system.
- Confirmed all 9 spec'd Lucide icons exist via a `node` probe (`Layers, Server, Smartphone, BrainCircuit, Cloud, Database, Boxes, ArrowRight, ArrowUpRight`). Mapped them 1:1 to the 7 categories: Frontend→Layers, Backend→Server, Mobile→Smartphone, AI & Data→BrainCircuit, Cloud→Cloud, Database→Database, DevOps→Boxes. Used `ArrowRight` + `ArrowUpRight` in the two intro CTAs ("See services" / "Start a project") so every imported icon is actually rendered (no unused-import lint error). Removed the previously-imported `PenTool` (it was used as the Frontend icon in the old page — replaced by `Layers` per spec).
- Confirmed the hero image asset `/images/wp/2025-02/technology1.png` exists (verified via `LS` on the directory).
- Rewrote `/src/app/(pages)/technologies/page.tsx` end-to-end with the new content + 7-card grid structure:
  1. **Hero** — `PageHero` with title "Technologies", label "Technologies", image `/images/wp/2025-02/technology1.png`, crumbs Home → Technologies (matches spec exactly).
  2. **Section 1 (intro, `bg-white`, 2-col)** — Left: `SectionHeading` with eyebrow `[ Tech stack ]`, title "Technologies", lead "Technology That Drives Digital Excellence", followed by the secondary subtext paragraph "We combine modern technologies, intelligent engineering, and scalable architectures to build secure, high-performance digital solutions for businesses worldwide." plus two CTA buttons ("See services" → `/services` brand-orange, "Start a project" → `/contact` outline). Right: editorial image (`/images/wp/2025-02/technology1.png`, 720×560) wrapped in `rounded-2xl shadow-float` with an offset `bg-cream` frame behind it (`absolute -left-5 -bottom-5 hidden h-full w-full rounded-2xl border border-line bg-cream lg:block`).
  3. **Section 2 (tech logos marquee, `bg-ink` navy)** — `grid-pattern` overlay + flame orb. Centered header: section-label `[ Stack proof ]` (forced `!text-brand-light` for navy legibility), h2 "The tools our engineers reach for.", and a supporting paragraph "Real tools, used in real production environments — across 250+ shipped projects and three offices worldwide." Then a flex-wrap of ALL 24 `technologiesGrid` items rendered as rounded glass chips: `border border-white/10 bg-white/[0.04] px-4 py-2 backdrop-blur` with `hover:border-flame/40 hover:bg-white/[0.07]`, each chip holding a 24×24 logo image (`unoptimized` to bypass the optimizer for these tiny transparent PNGs) + tech name.
  4. **Section 3 (7 category cards, `bg-shade`)** — `SectionHeading` centered with eyebrow `[ By category ]`, title "Seven categories. One engineering culture.", lead about day-one stack + team mapping. Then a `sm:grid-cols-2 lg:grid-cols-3` grid of 7 cards (the 7th naturally fills the first slot of the third row — clean rhythm with no awkward gap filler). Each card uses the established premium pattern: `card-lift` hover, signature 3px flame top-bar (`scale-x-0 → group-hover:scale-x-100`), soft cream glow in top-right corner on hover, `size-14` cream icon tile that flips to `bg-brand text-white` on hover (icon = `categoryIcons[i]`), `text-display-sm` category title, `text-body` description, and tech items rendered as chips (`rounded-full border border-line bg-shade px-3.5 py-1.5 text-xs font-medium text-ink/80` with brand-on-hover) pinned to the card bottom via `mt-auto`. **Removed the `01 / 05` numbered markers** that existed in the old page per the spec's "NO numbered markers" directive.
  5. **Section 4 (engineering principles, `bg-white`, 3-col)** — `SectionHeading` centered (eyebrow `[ Engineering principles ]`, title "How we choose what makes the cut.", lead about three rules). Then `md:grid-cols-3` grid of 3 cards (`principles` array defined inline with the exact spec copy: "Boring technology first" / "Owned, not rented" / "Versioned, observed, repeatable"). Each card uses `card-lift` hover, `bg-shade` panel with `border-line`, the signature `rule-flame` (44×3px gradient) at the top, `text-display-sm` title, and `text-body` description.
  6. **Section 5 (CTA, `tone="ink"`)** — `CTABand` with label `[ Stack match ]`, title "Want engineers who know your stack?", desc about matching senior engineers to your stack, `ctaHref="/contact"`, `ctaLabel="Start a project"`. Tone "ink" gives the navy gradient panel — completing the `bg-white` → `bg-ink` → `bg-shade` → `bg-white` → `bg-ink` alternation across the five sections.
- Updated `buildMetadata` description to reflect "Seven engineering categories" (the old description was generic), and refreshed keywords toward frontend/backend/mobile/AI/cloud/database/DevOps to mirror the spec'd 7 categories.
- Followed the established React-fragment (`<>…</>`) wrapper pattern used by every sibling page; nav + footer are already provided by `(pages)/layout.tsx` so wrapping again with `<PageShell>` would duplicate them. Removed the unused `PageBanner` import that was carried over from the old page.
- Ran `bun run lint` (`eslint .`) → clean, no errors. Also ran `bunx tsc --noEmit` filtered for `technologies` and `src/app` → no type errors in the page (the only TS errors are in the unrelated `examples/websocket/` and `skills/` directories — confirmed not in `src/`).

Stage Summary:
- `src/app/(pages)/technologies/page.tsx` fully rewritten to the new brand copy + 7-card grid + glass-chip marquee structure. Lint clean. Page imports and uses `PageHero`, `Reveal`, `SectionHeading`, `CTABand`, `buildMetadata`, `technologiesGrid`, `technologyCategories`, `next/image` (`Image`), `next/link` (`Link`), and all 9 spec'd Lucide icons (`Layers`, `Server`, `Smartphone`, `BrainCircuit`, `Cloud`, `Database`, `Boxes`, `ArrowRight`, `ArrowUpRight`).
- Section 1 implements the spec'd 2-col intro: left column with `[ Tech stack ]` eyebrow + "Technologies" title + "Technology That Drives Digital Excellence" lead + the secondary subtext paragraph + dual CTAs; right column with the editorial image (`/images/wp/2025-02/technology1.png`) wrapped in `shadow-float` with an offset `bg-cream` frame behind it.
- Section 2 implements the spec'd navy-bg tech logos marquee: `[ Stack proof ]` eyebrow + "The tools our engineers reach for." headline + 250+ shipped projects subtext, then a flex-wrap of ALL 24 `technologiesGrid` items as rounded glass chips (logo image + name) on `bg-ink` with `grid-pattern` overlay.
- Section 3 implements the spec'd 7-card grid on `bg-shade`: `[ By category ]` eyebrow + "Seven categories. One engineering culture." title (centered) + 7 category cards in `sm:grid-cols-2 lg:grid-cols-3` — each card has a cream icon tile that flips to brand on hover + category name + description + tech items as chips. Icons mapped 1:1: Frontend→Layers, Backend→Server, Mobile→Smartphone, AI & Data→BrainCircuit, Cloud→Cloud, Database→Database, DevOps→Boxes. **NO numbered markers** anywhere on the cards (the old page's `01 / 05` mono counters were removed per spec).
- Section 4 implements the spec'd 3-col principles on `bg-white`: three cards with `rule-flame` + heading + desc — "Boring technology first" / "Owned, not rented" / "Versioned, observed, repeatable".
- Section 5: `CTABand` `tone="ink"` linking to `/contact` with the spec'd copy ("Want engineers who know your stack?" + "Start a project").
- Design-token usage matches the established system: `bg-white` / `bg-shade` / `bg-ink` (via CTA `tone="ink"`) alternation, `card-lift` hover, `rule-flame`, `section-label`, `container-site`, `text-flame` (eyebrows via section-label default), `text-brand` (hover icon tiles + brand CTA), `text-ink` (headings), `text-body` (paragraphs), `grid-pattern` + glass chips (`bg-white/[0.04] backdrop-blur`) on the navy section. NO numbered markers on any cards per spec.
